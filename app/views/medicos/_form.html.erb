<%= form_with(model: medico, local: true) do |form| %>
  <% if medico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medico.errors.count, "error") %> prohibited this medico from being saved:</h2>

      <ul>
      <% medico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :CPF %>
    <%= form.text_field :CPF %>
  </div>

  <div class="field">
    <%= form.label :CRM %>
    <%= form.text_field :CRM %>
  </div>

  <div class="field">
    <%= form.label :NOME %>
    <%= form.text_field :NOME %>
  </div>

  <div class="field">
    <%= form.label :ENDERECO %>
    <%= form.text_area :ENDERECO %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
