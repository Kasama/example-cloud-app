<%= form_with(model: secretaria, local: true) do |form| %>
  <% if secretaria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(secretaria.errors.count, "error") %> prohibited this secretaria from being saved:</h2>

      <ul>
      <% secretaria.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ESTADO %>
    <%= form.text_field :ESTADO %>
  </div>

  <div class="field">
    <%= form.label :MUNICIPIO %>
    <%= form.text_field :MUNICIPIO %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
